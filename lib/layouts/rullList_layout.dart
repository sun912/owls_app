import 'package:flutter/material.dart';

class RullListLayout extends StatelessWidget {
  const RullListLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
