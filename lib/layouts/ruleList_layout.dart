import 'package:flutter/material.dart';

class RuleListLayout extends StatelessWidget {
  const RuleListLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Rule Item');
  }
}
